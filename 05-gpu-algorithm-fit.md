# どんなアルゴリズムが Bend の GPU に向くか（実測）

[04-gpu-execution.md](04-gpu-execution.md) の続き。「分割統治が向く」という粗い理解を、**4つの仮説を1つずつ壊す対照実験**で精密化した記録。

実験ソースは [examples/](examples/)、生の計測値は [examples/results.txt](examples/results.txt)。

## 環境と正しさの担保

- Apple M5 / 10 core / Metal / clang 21 / Bend 2.0.23
- 計測は全部 **best-of-3 の wall clock**
- **全変種で SEQ / PAR / GPU の3モードがビット一致**することを確認済み
- FWHT は (a) involution 則 `FWHT(FWHT(x)) == 2^d · x`、(b) **独立に書いた C 実装2本**（配列 in-place / 同形の木）と一致、の二重で検証
- Monte Carlo は C twin と一致し、π = 3.141700 が出ることを確認

---

## 当初の仮説

Bend の GPU が要求する条件を4つ立てた。

```
  H1  総計算量が十分            起動固定コスト ~90ms を償却できるか
  H2  分割統治で書ける          combine が O(1) か O(size) か
  H3  負荷が均等                work-stealing が無いので
  H4  命令列が一様              SIMD divergence を避ける
```

**結果: H4 は完全な誤りだった。H3 は「均等」の意味が違った。**

---

## 実験1: 演算強度 (H1)

Monte Carlo π 推定。完全な二分 fork 木、葉1つが K サンプル、combine は加算1回。d=20（104万葉）で K を振る。

| K | サンプル数 | C (1 core) | Bend SEQ | Bend PAR(10) | Bend GPU |
|---|---|---|---|---|---|
| 256 | 2.68億 | 0.471s | 0.482s | 0.071s | **0.087s** ← GPU が負け |
| 1024 | 10.7億 | 1.918s | 1.934s | 0.288s | 0.092s |
| 4096 | 42.9億 | 7.714s | 7.757s | 1.092s | **0.140s** ← C 比 55x |

### わかったこと

**(a) Bend の単一コアは C と誤差1%で同着。**

```
  C 0.471 / 1.918 / 7.714
  B 0.482 / 1.934 / 7.757     どれも +1%
```

README の「as fast as C on the CPU」は、この形の計算では文字通り本当。

**(b) GPU の時間がほとんど増えない。**

仕事が 16 倍になっても GPU は 0.087 → 0.140 秒（1.6 倍）。つまり **K=256 の時点では GPU の時間はほぼ全部が起動コスト**で、計算は誤差。

```
  総仕事量(1コア換算)   GPU 時間   内訳
  ─────────────────    ────────   ──────────────────────
  0.48s                0.087s     ほぼ全部が起動コスト
  7.7s                 0.140s     0.09s 起動 + 0.05s 計算
```

**→ 実務ルール: `!` を付けて得をするのは、その計算が単一コアで 1 秒以上かかるとき。** それ未満は起動コスト負けする。

---

## 実験2: merge-heavy な分割統治 (H2)

FWHT（高速 Walsh–Hadamard 変換）。fork の形は実験1と同じだが、**combine が O(subtree)** になる。

```
  fwht(N{a,b}) = N{ fwht(a)+fwht(b), fwht(a)-fwht(b) }   (要素ごと)
```

アフィン制約のせいで、和と差を**1パスで同時に返す**書き方が強制される（`a'` を2回使えないので）。結果的にキャッシュ効率の良い形になるのは副産物。

| d | n | C(配列 in-place) | C(木, malloc/free) | Bend SEQ | Bend PAR | Bend GPU |
|---|---|---|---|---|---|---|
| 20 | 105万 | 0.004s | 0.660s | 0.053s | 0.016s | 0.108s |
| 22 | 419万 | 0.010s | 3.187s | 0.241s | 0.056s | 0.165s |
| 24 | 1678万 | **0.040s** | 9.951s | 0.909s | 0.177s | **0.440s** |

### わかったこと

**(a) 同じデータ構造なら Bend は C を圧倒する。**

```
  d=24、どちらもポインタ連結の木、同じアルゴリズム:

     C (malloc/free)   9.951s
     Bend SEQ          0.909s     ← 11 倍速い
```

アフィン型による freelist アロケータが、汎用 malloc/free の 11 倍速い。しかも C 側は解放を手で書く必要があり、arena で逃げようとすると d=24 で **19GB** 必要になる（Bend は自動で解放するので不要）。

**(b) しかし正しいデータ構造は配列で、Bend はそれを書けない。**

```
  d=24:
     C 配列 in-place    0.040s   ← SIMD + 帯域律速、確保ゼロ
     Bend GPU           0.440s   ← 11 倍遅い
     Bend SEQ           0.909s   ← 22 倍遅い
```

FWHT は 2 ロード 2 ストアに対して 1 加算 1 減算、つまり**演算強度が最低クラス**で完全に帯域律速。C は in-place で 4-wide SIMD、メモリ帯域の限界（M5 で ~160GB/s）に張り付く。

Bend はアフィン型ゆえに配列のエイリアスを持てず、木をヒープに materialize するしかない。**GPU に載れる理由（エイリアスが無い）が、そのまま最良アルゴリズムを禁止している。**

**(c) GPU の効きも悪い。** Bend SEQ → GPU が 2.1 倍にしかならない（実験1では 55 倍）。

```
  ┌──────────────────────────────────────────────────────────┐
  │  H2 の結論:                                               │
  │  combine が O(size) なこと自体は致命的ではない。          │
  │  致命的なのは「配列で in-place に書ける問題」であること。  │
  │  その場合 C の配列版が最初から桁で速く、Bend は勝てない。  │
  └──────────────────────────────────────────────────────────┘
```

---

## 実験3: 負荷の置き方 (H3) — 予測が外れてモデルを直した

**総仕事量を完全に同じに保ったまま**、葉への仕事の配り方だけを変える。d=20、平均 K=4096。

| 変種 | 配り方 | SEQ | PAR(10) | GPU |
|---|---|---|---|---|
| uniform | 全葉が 4096 サンプル | 8.114s | 1.092s | **0.132s** |
| hot 1/64 | `i % 64 == 0` の葉だけが 64×K | 7.948s | 1.086s | **0.131s** |
| linear ramp | 葉 i が `i>>7` サンプル | 7.878s | 1.093s | 0.184s |
| block 1/64 | `i < n/64` の葉だけが 64×K | 8.210s | 2.086s | **1.636s** |

SEQ は全部 8 秒前後。総仕事量が同じことの確認になっている。**GPU だけが 0.131s 〜 1.636s、12.5 倍のばらつき。**

### 予測が外れた

当初 `hot 1/64`（64葉に1つだけが全部の仕事をする）が最悪になると予測した。実際は **uniform と完全に同じ**だった。一方 `block 1/64`（先頭 1/64 の範囲が全部の仕事をする）が 12.4 倍遅かった。

両者は「64 葉に 1 葉ぶんの仕事」という意味では同じ偏り方なのに、結果が真逆。

### 正しいモデル

```
  lane には index の剰余ではなく、連続した部分木が割り当てられる。

  1M 葉 / 16384 lane = 1 lane あたり連続 64 葉

  hot (剰余):   lane ごとの 64 葉の中に、必ず 1 個ずつ hot 葉がある
                → 完全に均等。ペナルティ 0

     lane0: [■□□□...□]  lane1: [■□□□...□]  lane2: [■□□□...□]   全部同じ

  block (範囲): 先頭 256 lane が全部の仕事を持ち、残り 16128 lane が遊ぶ

     lane0..255: [■■■■...■]   lane256..16383: [□□□□...□]        12.4x 遅い
```

**→ 実務ルール: 偏りが index の「剰余」に相関するなら無料。「範囲」に相関するなら致命的。**

CPU (PAR) も block で 1.09 → 2.09 と 1.9 倍悪化するが、10 スレッドしかないぶん回復が効く。lane が 16384 個ある GPU の方が、この種の偏りに対してはるかに脆い。

---

## 実験4: divergence (H4) — 仮説が完全に間違っていた

葉ごとのサンプルで、乱数の1ビットによって**重い経路（64 ラウンドのハッシュ）**と**軽い経路（0 ラウンド）**を分岐する。対照は常に 32 ラウンド。**平均仕事量は同一。**

| 変種 | SEQ | PAR(10) | GPU |
|---|---|---|---|
| uniform 32 rounds | 7.814s | 1.359s | 0.112s |
| divergent 64/0 | **10.663s** | 1.867s | **0.104s** |
| 比 | **1.36x 悪化** | 1.37x 悪化 | **0.93x（悪化なし）** |

### 結果

**CPU は 36% 悪化し、GPU は全く悪化しない。** 予測と正反対。

CPU 側はランダム分岐なので分岐予測が 50% 外れ、パイプラインフラッシュを食らう。GPU 側はゼロ。

### なぜか

Bend の GPU 実行は**古典的な SIMT ではない**から。`comp.ts` の実体は、各 lane が自分のリングからタスクを取って `switch` でディスパッチする worklist ステートマシン。

```
  【古典的な GPU カーネル】              【Bend の worklist machine】

  32 lane が同じ命令ポインタで          32 lane がそれぞれ別のタスクの
  lockstep 実行                         別の地点を実行している

  分岐すると両方の枝を直列化            そもそも最初から揃っていない
       ↓                                      ↓
  divergence が 2x のコスト            追加の divergence は実質無料
```

つまり **Bend は GPU スレッドを「ベクトルレーン」ではなく「独立スレッド」として使っている**。

これが「汎用言語が GPU で動く」ことの、もう一つの答えです。[04](04-gpu-execution.md) では「GC が無いから載る」と書いたが、正確には **GC が無いこと + SIMD 一様性を要求しないこと**の両方。

同時にこれは **GPU 性能の天井の説明でもある**。SIMD 幅を使い切っていないので、手書き Metal/CUDA には届かない。実測が 1 コア C 比 10〜100 倍に収まり、1000 倍にならないのはそのため。

---

## 改訂版: 向くアルゴリズムの条件

```
  ┌───────────────────────────────────────────────────────────────┐
  │  ✓ 必須 1  単一コアで 1 秒以上かかる                           │
  │            (GPU 起動の固定コスト ~90ms を償却するため)         │
  │                                                                │
  │  ✓ 必須 2  仕事量が index の「範囲」に対して均等               │
  │            (剰余に対する偏りは無害。範囲に対する偏りは 12x)    │
  │                                                                │
  │  ✓ 必須 3  配列 in-place の定式化が存在しない、               │
  │            または演算強度が高くて帯域律速にならない             │
  │            (存在するなら C の配列版に桁で負ける)               │
  │                                                                │
  │  ✗ 不要    命令列の一様性                                      │
  │            (divergence のコストはゼロ。CPU より強いくらい)     │
  └───────────────────────────────────────────────────────────────┘
```

### 向く / 向かない

| 向く | 理由 |
|---|---|
| Monte Carlo / 確率的シミュレーション | 演算強度が無限大（メモリを触らない）、完全均等 |
| N体・粒子法 | 既存ベンチで C 比 87x |
| レイトレーシング・SDF | divergence が無料なので枝分かれし放題 |
| セルオートマトン | 既存ベンチで C 比 108x |
| 木の構築・走査・畳み込み | アフィン型の本領。readback も無料 |
| 探索木（分岐限定法など） | divergence 無料が効く。ただし枝刈りで範囲偏りが出ると死ぬ |

| 向かない | 理由 |
|---|---|
| FFT / FWHT / 畳み込み | 配列 in-place + SIMD が正解で、C に 11x 負ける |
| ソート | 同上。既存ベンチの tree-bitonic も GPU 12.7x どまり |
| 字句解析・文字列処理 | 逐次的。既存ベンチで GPU が CPU に 5.4x 負ける |
| ハッシュマップ | エイリアスが本質。既存ベンチで C に 4.4x 負ける |
| 逐次 fold / 状態機械 | 実測で CPU の 100x 遅い |
| 小さい計算 | 起動コスト負け |

---

## おまけ: Bend を書いていて実際に詰まった点

実験中に 4 回コンパイラに止められた。全部アフィン型か、その周辺。

| 症状 | 原因 | 直し方 |
|---|---|---|
| `v (consumed more than once)` | `U32.add(u,v)` と `U32.sub(u,v)` で v を2回使った | パターンを `case L{+u} L{+v}` に |
| 同上 | `draw(p, h, U32.and(h,1))` で h を2回 | `+h = ...` に |
| `expected: a defined name` | `bfly.join` を使用箇所より後に定義した | **相互再帰禁止**。先に定義する |
| `expected: a defined name: Nat.pred` | 存在しない | `U32.shrn(U32.shln(1,d),1n)` で代用 |

加えて、**U32 のオーバーフローが黙って実験を壊した**（`2*K*i` が 2^32 を超えて総仕事量が半減し、偽の「偏りは速い」という結果が出た）。型システムは所有権は守るが算術オーバーフローは守らない。`U32.add_comm` のような法は base にあるが、オーバーフローしないことは証明の対象外。

### 所感

アフィン制約は思ったより邪魔にならない。4回止められたうち3回は「2回使った」で、直し方は `+` を付けるだけ。しかも FWHT では、アフィン性に従って書いた結果が**そのままキャッシュ効率の良い1パス butterfly** になった。制約が良い設計に誘導している側面はある。

一方で相互再帰の禁止は素直に面倒で、定義順を手で並べ替える必要があった。
