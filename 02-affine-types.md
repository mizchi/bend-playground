# アフィン型 (affine types) とは何か

一行で言うと **「値は高々 1 回しか使えない。捨てるのは自由」**。Rust の move semantics を知っていれば、それがほぼアフィン型です。

---

## 1. 普通の言語: 値は何回でも使える

```python
x = [1, 2, 3]
f(x)
g(x)        # x をもう一度使える
print(x)    # 何回でも
```

これが当たり前に見えるのは、裏で**参照が複製されている**からです。

```
      x ──┐
          ├──> [1,2,3]   1 個の実体に 3 本の参照 (エイリアス)
   f(x) ──┤
          │
   g(x) ──┘
```

エイリアスがあると、次の問題が全部ついてくる:

- **いつ解放していいか分からない** → GC か参照カウントが要る
- **破壊的更新が危険** → `f` が書き換えたら `g` から見える
- **並行アクセスが危険** → データ競合。ロックが要る

---

## 2. 線形型とアフィン型

| | 使用回数 | 例 |
|---|---|---|
| **通常** | 0 回以上、何回でも | Python, Java, Haskell |
| **アフィン型** | **高々 1 回** (0 か 1) | Rust (move), **Bend** |
| **線形型** | **ちょうど 1 回** | Linear Haskell, Clean |

線形型は「捨ててもいけない」ので、ファイルハンドルのように「必ず close させたい」ものに向く。アフィン型は捨てるのが自由なので、書き味がだいぶ楽になる。

```
  線形 (linear)              アフィン (affine)
  ─────────────              ────────────────
  x を必ず 1 回使え           x を使わなくてもいい
  ✗ let x = v; ()            ✓ let x = v; ()
  ✓ let x = v; f(x)          ✓ let x = v; f(x)
  ✗ let x = v; f(x); g(x)    ✗ let x = v; f(x); g(x)
```

---

## 3. アフィン型だと何が変わるか

```
  【通常】 エイリアスあり              【アフィン】 エイリアスなし

      x ──┐                              x ──> [1,2,3]
          ├──> [1,2,3]                        │
   f(x) ──┤                                   │ f(x) が所有権を取る
          │                              f(x) ──> [1,2,3]
   g(x) ──┘                                   │
                                              │ g(x) はもう x を使えない
   誰が最後に使うか不明            g(x) ──> ✗ コンパイルエラー
       ↓
   GC が必要                       最後の使用地点が静的に分かる
                                       ↓
                                   free をコンパイラが置ける = GC 不要
```

これが Bend の全ての土台です。

### 3 つの帰結

**(1) GC が要らない**

値の最後の使用地点がコンパイル時に確定するので、コンパイラが `free` を配置できる。Bend の回収器は `term_drop` (`comp.ts:4102`) ただ 1 つで、トレーシングも全域の参照カウントもない。→ これが **GPU に載る決定的な理由** ([04](04-gpu-execution.md))

**(2) 破壊的更新が安全**

配列に他から参照がないと保証されているので、`set` をその場で書き換えても誰も困らない。永続データ構造のコピーが要らない。

```python
# Bend の Array.map は「ブロックを歩いて新しい配列に書き込む」get/set ループ
# これが純粋関数として安全なのは、入力配列に別名が存在しないから
```

**(3) データ競合が原理的に起きない**

2 つのスレッドが同じ値への参照を持てない。だから**ロックが要らない**。Bend が「no threads, no locks」と言えるのはこれ。

---

## 4. Bend の実装: 使用量マップ

Bend はアフィン性を「文脈分割」ではなく**使用量マップ**で実装しています (`bend2/bend.ts:592-619`)。

3 段階の量 (quantity) がある:

| 記法 | 名前 | 意味 |
|---|---|---|
| `-x` | **None** | 消去 (erased)。型・証明の中でだけ使える。実行時に消える |
| `x` | **Lone** | 高々 1 回 (デフォルト) |
| `+x` | **Many** | 何回でも使える。**ただし型が `Data` の時だけ** |

検査のしかたは素朴で、それが速さの理由でもある:

```
  各変数の使用回数を足していく:   Lone + Lone = Many
  分岐は各枝の最大を取る:         join = pointwise max
  束縛子を閉じるときに検査:       測定値 ≤ 宣言値 でなければエラー
```

```
        def f(x: Nat, +y: Nat) -> Nat:      宣言: x=Lone, y=Many
          a = Nat.add(x, y)                 使用: x=Lone, y=Lone
          Nat.add(a, y)                     使用: y をもう一回 → y=Many
                                            ────────────────────────
                                            x: Lone ≤ Lone  OK
                                            y: Many ≤ Many  OK

        def g(x: Nat) -> Nat:               宣言: x=Lone
          Nat.add(x, x)                     使用: x=Many
                                            ────────────────────────
                                            x: Many ≤ Lone  ✗ エラー
```

---

## 5. `Data` と `Type` の区別が全て

ここが Bend で一番重要な一行です。

```
  すべての型には kind がある:  Kind(q)  (q は量)

     Type = Kind(&1)   ← 高々 1 回しか使えないものの kind
     Data = Kind(&2)   ← 何回でも使えるものの kind

  +x: A と書けるのは A : Data のときだけ
```

そして:

```
  ┌────────────────────────────────────────────────┐
  │  関数型は 絶 対 に Data にならない              │
  │  (bend2/bend.ts / bend.lean のヘッドライン)     │
  └────────────────────────────────────────────────┘
```

つまり **`+f: (A -> B)` とは書けない**。関数値は必ず高々 1 回。

なぜこれが効くのか → [03-bend-wall.md](03-bend-wall.md)

### 実行時に所有権を持つものも Type 側

`File` / `Socket` / `Array` は `Type` であって `Data` ではない。だから複製できない。README の「closures and arrays cannot be shared」はこれです。配列の破壊的 `set` が健全なのも、この区別のおかげ。

---

## 6. Rust との違い

| | Rust | Bend |
|---|---|---|
| 所有権 | あり (move) | あり (affine) |
| 借用 (borrow) | `&T` / `&mut T` がある | **ない** |
| 複製 | `Copy` / `Clone` trait | `+x` (型が `Data` のときのみ) |
| 目的 | メモリ安全 | メモリ安全 **+ 論理的無矛盾性 + GPU 実行** |

Rust に借用があるのは実用性のため。Bend には借用がなく、代わりに `+` による「callee 側でコピーする」方式 (certify-once) を採っています。ここは QTT (Quantitative Type Theory) が禁じている操作で、Bend のヘッダ自身が「その代償として subject reduction は closed term の値呼び簡約に限定される」と認めています (`bend.ts:176-190`)。

---

## 7. アフィン型の代償

タダではない。README の Limitations にそのまま出ている。

- 文字列が連結リストなので**テキスト処理が遅い** (`lexer` ベンチが C の 2.07 倍遅い直接の原因)
- ハッシュマップが永続木になる (`hashmap` ベンチが C の **4.41 倍遅い**。16 本中の最悪値)
- 値を 2 回使いたいときに `+` を付けるか、明示的に複製するか考える必要がある
- 配列を並行で共有するには `@unsafe` が要る (`Array.fork` / `Array.join`, `base.bend:2285`)

つまり **「エイリアスが本質的に必要なデータ構造」が苦手**。逆に木構造・分割統治は最強。ベンチで tree 系だけが C に勝っている (`tree-matmul` は C の 0.70 倍 = 勝ち) のはそのためです。
